from qdpy import algorithms, containers, benchmarks, plots
import run_oxdna as rox
import run_peppercorn as rpc

def eval_oxdna_directly(indiv, length_a=6, length_b=7, dirpath="./"):
    args = np.argsort(indiv)
    arrs = np.zeros(len(indiv))
    for i in args[-7:]:
        if array[i] > th:
            arrs[i] = 1.0#数字のリストIndividualをを0,1の値のリストに変換する。
    
    strands_list = 
    
    rpc.make_peppercorn_input(dirpath,length_a,length_b,strands_list,indiv.name+".pil")
if __name__ == "__main__":
    
    #First we need to make the algo
    pass